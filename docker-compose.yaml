version: '3.8'

services:
  abdm-wrapper:
    build:
      context: .
      dockerfile: Dockerfile
    image: abdm-wrapper:latest
    ports:
      - '8082:8082'
    depends_on:
      - mongodb
    environment:
      SPRING_DATA_MONGODB_URI: ${SPRING_DATA_MONGODB_URI:-mongodb://host.docker.internal:27017/abdm}
    networks:
      - docker_network
    extra_hosts:
      - "host.docker.internal:host-gateway"

  mongodb:
    image: mongodb/mongodb-community-server:latest
    restart: always
    ports:
      - '27017:27017'
    volumes:
      - mongodb_vol:/data/db
    networks:
      - docker_network

volumes:
  mongodb_vol:

networks:
  docker_network:
    driver: bridge
    driver_opts:
      com.docker.network.driver.mtu: 1500